# Runtime plugin drop folder

Place compiled plugin DLLs here (next to the LanServer exe). See `../Plugins/README.md`.

Per-plugin data: `./{PluginName}/config.json`

Ignore local secrets / built DLLs in git when this folder is used as a source tree copy.
